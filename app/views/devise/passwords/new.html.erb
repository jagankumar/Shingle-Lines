<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">

</head>
<body>

<center><h2>Forgot your password?</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <div class="container">
  <%= render "devise/shared/error_messages", resource: resource %>
    <%= f.label :email,:class=>"field-lable" %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email",:class =>"pass-field" ,:placeholder=>"Enter Email" %>
    <%= f.submit "Send me reset password instructions" ,:class=>"registerbtn"%>

<% end %>

<%= render "devise/shared/links" %>
</div></center>
</body>
</html>